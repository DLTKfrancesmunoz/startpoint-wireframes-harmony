---
/**
 * ButtonGroup Component
 *
 * Group of related buttons displayed together.
 * Default slot: consumer-provided Button children. No fallback; consumer must pass content for a meaningful default state.
 */

export interface Props {
  variant?: 'default' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  orientation?: 'horizontal' | 'vertical';
  class?: string;
}

const {
  variant = 'default',
  size = 'md',
  orientation = 'horizontal',
  class: className = '',
} = Astro.props;

const classes = [
  'btn-group',
  `btn-group--${variant}`,
  `btn-group--${size}`,
  `btn-group--${orientation}`,
  className
].filter(Boolean).join(' ');
---

<div class={classes} role="group">
  <slot />
</div>
